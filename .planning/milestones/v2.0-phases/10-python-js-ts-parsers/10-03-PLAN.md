---
phase: 10-python-js-ts-parsers
plan: 03
type: execute
wave: 2
depends_on: [10-01, 10-02]
files_modified:
  - internal/code/analyzer.go
  - internal/code/integration.go
  - internal/code/integration_test.go
  - cmd/graphmd/main.go
  - internal/knowledge/export.go
  - internal/knowledge/crawl_cmd.go
autonomous: true
requirements: [CODE-02, CODE-03]

must_haves:
  truths:
    - "Running --analyze-code on a directory with Python files produces Python CodeSignal output"
    - "Running --analyze-code on a directory with JS/TS files produces JavaScript CodeSignal output"
    - "Running --analyze-code on a mixed-language directory (Go+Python+JS) produces signals from all three parsers"
    - "InferSourceComponent returns the package name from pyproject.toml, setup.py, or package.json"
    - "AnalyzeDir skips __tests__, __pycache__, .venv, venv, .tox directories"
  artifacts:
    - path: "internal/code/analyzer.go"
      provides: "Extended skipDirs and InferSourceComponent for Python and JS"
      contains: "__pycache__"
    - path: "internal/code/integration_test.go"
      provides: "End-to-end tests for Python and JS parser registration"
      contains: "pyparser"
    - path: "cmd/graphmd/main.go"
      provides: "All three parsers registered at CLI call site"
      contains: "jsparser.NewJSParser()"
  key_links:
    - from: "cmd/graphmd/main.go"
      to: "internal/code/pyparser/parser.go"
      via: "import and NewPythonParser() call"
      pattern: "pyparser\\.NewPythonParser"
    - from: "cmd/graphmd/main.go"
      to: "internal/code/jsparser/parser.go"
      via: "import and NewJSParser() call"
      pattern: "jsparser\\.NewJSParser"
    - from: "internal/knowledge/export.go"
      to: "internal/code/pyparser/parser.go"
      via: "import and NewPythonParser() call"
      pattern: "pyparser\\.NewPythonParser"
    - from: "internal/knowledge/crawl_cmd.go"
      to: "internal/code/jsparser/parser.go"
      via: "import and NewJSParser() call"
      pattern: "jsparser\\.NewJSParser"
---

<objective>
Register Python and JS/TS parsers alongside GoParser at all CLI call sites, extend InferSourceComponent for Python and JS project manifests, and add Python/JS-specific directories to AnalyzeDir's skip list.

Purpose: Complete the integration so `--analyze-code` detects dependencies across Go, Python, and JS/TS codebases with a single command.
Output: Updated CLI commands, extended source component inference, and integration tests.
</objective>

<execution_context>
@/Users/flurryhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flurryhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-python-js-ts-parsers/10-01-SUMMARY.md
@.planning/phases/10-python-js-ts-parsers/10-02-SUMMARY.md
@internal/code/analyzer.go
@internal/code/integration.go
@internal/code/integration_test.go
@cmd/graphmd/main.go
@internal/knowledge/export.go
@internal/knowledge/crawl_cmd.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend AnalyzeDir skip directories and InferSourceComponent for Python/JS</name>
  <files>internal/code/analyzer.go</files>
  <action>
    **1. Extend skipDirs map** in analyzer.go to include Python and JS ecosystem directories:
    ```go
    var skipDirs = map[string]bool{
        "vendor":       true,
        "node_modules": true,
        ".git":         true,
        "__tests__":    true,
        "__pycache__":  true,
        ".venv":        true,
        "venv":         true,
        ".tox":         true,
        "dist":         true,
        "build":        true,
    }
    ```

    **2. Extend InferSourceComponent** to check Python and JS project manifests after go.mod, in this order:
    - `go.mod` (existing, keep as-is)
    - `pyproject.toml` — extract name from `[project]` section using regex: `(?m)^name\s*=\s*"([^"]+)"` (TOML parsing with regex is sufficient for this single field)
    - `setup.py` — extract name from `name="..."` or `name='...'` argument using regex
    - `package.json` — unmarshal JSON into `struct { Name string }` and return Name
    - Fall back to `filepath.Base(dir)` (existing)

    The walk-up-directory logic already exists for go.mod. Extend the loop body to check each manifest file in order. Stop at the first match found (don't keep walking up for other manifest types once one is found at a given level — but DO keep walking up if no manifest is found at the current level).

    Add `encoding/json` to imports for package.json parsing. Keep the existing `golang.org/x/mod/modfile` import for go.mod.
  </action>
  <verify>
    <automated>cd /Users/flurryhead/Developer/Opensource/graphmd && go test ./internal/code/ -v -run TestInfer -count=1</automated>
    <manual>Verify skipDirs contains all Python/JS directories</manual>
  </verify>
  <done>InferSourceComponent returns correct names from pyproject.toml, setup.py, and package.json. skipDirs includes __tests__, __pycache__, .venv, venv, .tox, dist, build.</done>
</task>

<task type="auto">
  <name>Task 2: Register Python and JS parsers at CLI call sites and add integration tests</name>
  <files>cmd/graphmd/main.go, internal/knowledge/export.go, internal/knowledge/crawl_cmd.go, internal/code/integration_test.go</files>
  <action>
    **1. Update all three RunCodeAnalysis call sites** to pass all three parsers:

    In `cmd/graphmd/main.go` (line ~181):
    ```go
    signals, codeErr := code.RunCodeAnalysis(absDir,
        goparser.NewGoParser(),
        pyparser.NewPythonParser(),
        jsparser.NewJSParser(),
    )
    ```
    Add imports for `pyparser` and `jsparser` packages.

    In `internal/knowledge/export.go` (line ~228): Same pattern. Add imports.

    In `internal/knowledge/crawl_cmd.go` (line ~151): Same pattern. Add imports.

    **2. Add integration tests** in `internal/code/integration_test.go`:

    Test `TestRunCodeAnalysisPython`: Create temp dir with a `pyproject.toml` (name = "my-python-svc") and a Python file containing `import requests` + `requests.get("http://payment-api:8080/pay")`. Run `RunCodeAnalysis` with `pyparser.NewPythonParser()`. Assert at least 1 signal with Language="python", DetectionKind="http_call".

    Test `TestRunCodeAnalysisJavaScript`: Create temp dir with a `package.json` (name = "my-js-svc") and a JS file containing `import axios from 'axios'` + `axios.get("http://order-api:3000/orders")`. Run `RunCodeAnalysis` with `jsparser.NewJSParser()`. Assert at least 1 signal with Language="javascript", DetectionKind="http_call".

    Test `TestRunCodeAnalysisMultiLanguage`: Create temp dir with Go, Python, and JS files + `go.mod`. Run `RunCodeAnalysis` with all three parsers. Assert signals from all three languages are present.

    Test `TestInferSourceComponentPython`: Create temp dir with `pyproject.toml` containing `name = "my-py-app"`. Assert `InferSourceComponent` returns "my-py-app".

    Test `TestInferSourceComponentJS`: Create temp dir with `package.json` containing `{"name": "my-js-app"}`. Assert `InferSourceComponent` returns "my-js-app".

    Import both `pyparser` and `jsparser` packages in the test file (external test package `code_test` avoids import cycles).
  </action>
  <verify>
    <automated>cd /Users/flurryhead/Developer/Opensource/graphmd && go test ./internal/code/ -v -count=1 && go test ./cmd/graphmd/ -v -count=1 2>/dev/null; go vet ./...</automated>
  </verify>
  <done>All three CLI commands register Go, Python, and JS parsers. Integration tests prove multi-language analysis works end-to-end. go vet passes with no import cycle errors.</done>
</task>

</tasks>

<verification>
```bash
# Full test suite
cd /Users/flurryhead/Developer/Opensource/graphmd && go test ./internal/code/... -v -count=1

# Verify all parsers compile and register
go vet ./...

# Verify no import cycles
go build ./cmd/graphmd/
```
</verification>

<success_criteria>
- All three parsers registered at all three CLI call sites
- InferSourceComponent works for go.mod, pyproject.toml, setup.py, package.json
- AnalyzeDir skips Python/JS ecosystem directories
- Integration tests pass for Python, JS, and multi-language analysis
- go vet and go build clean (no import cycles)
- All existing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/10-python-js-ts-parsers/10-03-SUMMARY.md`
</output>
