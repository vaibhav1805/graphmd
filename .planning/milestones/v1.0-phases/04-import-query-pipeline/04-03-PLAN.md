---
phase: 04-import-query-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/knowledge/query_cli.go
  - internal/knowledge/query_cli_test.go
autonomous: true
gap_closure: true
requirements:
  - IMPORT-01
  - IMPORT-02
  - IMPORT-03

must_haves:
  truths:
    - "metadata.graph_name in query JSON output contains the name of the queried graph, not an empty string"
    - "TestQueryEnvelope_Structure asserts graph_name is present and non-empty"
  artifacts:
    - path: "internal/knowledge/query_cli.go"
      provides: "buildMetadata accepts graphName parameter and sets GraphName field"
      contains: "m.GraphName = graphName"
    - path: "internal/knowledge/query_cli_test.go"
      provides: "Assertion for graph_name field in envelope structure test"
      contains: "graph_name"
  key_links:
    - from: "cmdQueryImpact"
      to: "buildMetadata"
      via: "*graphName passed as argument"
      pattern: "buildMetadata\\(g, meta, \\*graphName, elapsed\\)"
    - from: "cmdQueryDependencies"
      to: "buildMetadata"
      via: "*graphName passed as argument"
      pattern: "buildMetadata\\(g, meta, \\*graphName, elapsed\\)"
    - from: "cmdQueryPath"
      to: "buildMetadata"
      via: "*graphName passed as argument"
      pattern: "buildMetadata\\(g, meta, \\*graphName, elapsed\\)"
    - from: "cmdQueryList"
      to: "buildMetadata"
      via: "*graphName passed as argument"
      pattern: "buildMetadata\\(g, meta, \\*graphName, elapsed\\)"
---

<objective>
Fix metadata.graph_name field in query JSON output — currently always empty string.

Purpose: The CONTEXT.md contract requires graph_name in metadata so AI agents can identify which graph was queried. The field is declared in QueryEnvelopeMetadata but buildMetadata() never populates it.

Output: All four query subcommands emit graph_name in their JSON metadata. Test coverage ensures this doesn't regress.
</objective>

<execution_context>
@/Users/flurryhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flurryhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-import-query-pipeline/04-02-SUMMARY.md
@.planning/phases/04-import-query-pipeline/04-VERIFICATION.md
@internal/knowledge/query_cli.go
@internal/knowledge/query_cli_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire graph name through buildMetadata and add test assertion</name>
  <files>internal/knowledge/query_cli.go, internal/knowledge/query_cli_test.go</files>
  <action>
    In query_cli.go:

    1. Change buildMetadata signature from `buildMetadata(g *Graph, meta *ExportMetadata, elapsedMs int64)` to `buildMetadata(g *Graph, meta *ExportMetadata, graphName string, elapsedMs int64)`.

    2. Inside buildMetadata, after the existing field assignments, add: `m.GraphName = graphName`.

    3. Update all four call sites to pass `*graphName` as the third argument:
       - Line ~217 (cmdQueryImpact): `buildMetadata(g, meta, *graphName, elapsed)`
       - Line ~286 (cmdQueryDependencies): `buildMetadata(g, meta, *graphName, elapsed)`
       - Line ~386 (cmdQueryPath): `buildMetadata(g, meta, *graphName, elapsed)`
       - Line ~486 (cmdQueryList): `buildMetadata(g, meta, *graphName, elapsed)`

    In query_cli_test.go:

    4. In TestQueryEnvelope_Structure, add "graph_name" to the metadata field check list (the `[]string{"graph_version", "created_at", ...}` slice). Also add an assertion that graph_name is non-empty (since the test uses `--graph query-test`, the value should be "query-test").
  </action>
  <verify>
    <automated>cd /Users/flurryhead/Developer/Opensource/graphmd && go test ./internal/knowledge/ -run TestQueryEnvelope_Structure -v</automated>
    <manual>Verify graph_name appears in JSON output by running: go test ./internal/knowledge/ -run TestQuery -v</manual>
  </verify>
  <done>buildMetadata receives and sets graphName; all four subcommands pass *graphName; TestQueryEnvelope_Structure asserts graph_name is present and equals "query-test"</done>
</task>

</tasks>

<verification>
1. `go test ./internal/knowledge/ -run TestQuery -v` — all 13+ query tests pass
2. `go test ./internal/knowledge/ -run TestQueryEnvelope_Structure -v` — graph_name field verified
3. `go vet ./internal/knowledge/` — no type errors from signature change
</verification>

<success_criteria>
- metadata.graph_name is populated in JSON output for all four query types
- TestQueryEnvelope_Structure explicitly checks graph_name field
- All existing tests continue to pass (no regressions from signature change)
</success_criteria>

<output>
After completion, create `.planning/phases/04-import-query-pipeline/04-03-SUMMARY.md`
</output>
