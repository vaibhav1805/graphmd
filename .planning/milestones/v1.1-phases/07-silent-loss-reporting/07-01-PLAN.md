---
phase: 07-silent-loss-reporting
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/knowledge/query_cli.go
  - internal/knowledge/query_cli_test.go
autonomous: true
requirements: [DEBT-02]

must_haves:
  truths:
    - "Impact query on a cyclic graph returns cycles_detected in JSON metadata"
    - "Dependencies query on a cyclic graph returns cycles_detected in JSON metadata"
    - "Queries on acyclic graphs produce no cycles_detected field (omitempty)"
    - "BFS distances remain correct despite cycle detection (no switch to DFS)"
    - "Root node is not falsely reported as a cycle participant"
  artifacts:
    - path: "internal/knowledge/query_cli.go"
      provides: "CycleEntry struct and cycles_detected field on QueryEnvelopeMetadata"
      contains: "CyclesDetected"
    - path: "internal/knowledge/query_cli_test.go"
      provides: "Cycle detection tests with cyclic graph fixtures"
      contains: "setupCyclicQueryTestGraph"
  key_links:
    - from: "internal/knowledge/query_cli.go"
      to: "QueryEnvelopeMetadata"
      via: "CyclesDetected []CycleEntry with omitempty tag"
      pattern: "cycles_detected,omitempty"
    - from: "executeImpactReverse"
      to: "CycleEntry"
      via: "back-edge detection in BFS visited check"
      pattern: "cycles = append"
    - from: "executeForwardTraversal"
      to: "CycleEntry"
      via: "back-edge detection in BFS visited check"
      pattern: "cycles = append"
---

<objective>
Add cycle back-edge detection to BFS traversal functions and surface detected cycles in query JSON envelope metadata.

Purpose: AI agents consuming impact/dependencies queries need to know when cycles exist in the dependency graph so they can interpret results correctly (cycles mean some paths are infinite).
Output: CycleEntry struct, updated BFS functions returning cycle info, cycles_detected field in JSON envelope.
</objective>

<execution_context>
@/Users/flurryhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flurryhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-silent-loss-reporting/07-RESEARCH.md
@internal/knowledge/query_cli.go
@internal/knowledge/query_cli_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CycleEntry struct, update BFS functions to detect and return cycle back-edges, add CyclesDetected to metadata</name>
  <files>internal/knowledge/query_cli.go, internal/knowledge/query_cli_test.go</files>
  <action>
**RED phase — write failing tests first:**

Create a `setupCyclicQueryTestGraph` helper in `query_cli_test.go` that builds a graph with at least one cycle. Example: add an edge from `primary-db` back to `payment-api` to create the cycle `payment-api -> primary-db -> payment-api`. Reuse the pattern from the existing `setupQueryTestGraph` helper.

Write tests that:
1. Run impact query on a cyclic graph and assert `metadata.cycles_detected` is non-empty with correct `from`/`to` fields
2. Run dependencies query on a cyclic graph and assert `metadata.cycles_detected` is non-empty
3. Run impact query on the existing acyclic test graph and assert `cycles_detected` is nil/absent
4. Verify BFS distances are still correct on cyclic graph (cycle detection doesn't corrupt traversal)
5. Verify root node is NOT reported as a cycle participant (guard: `edge.Source != root` for reverse, `edge.Target != root` for forward)

Run tests — they must FAIL (CycleEntry doesn't exist yet, BFS functions don't return cycles).

**GREEN phase — implement:**

In `query_cli.go`:

1. Add `CycleEntry` struct:
```go
type CycleEntry struct {
    From string `json:"from"`
    To   string `json:"to"`
}
```

2. Add `CyclesDetected` field to `QueryEnvelopeMetadata`:
```go
CyclesDetected []CycleEntry `json:"cycles_detected,omitempty"`
```

3. Update `executeImpactReverse` return signature to include `[]CycleEntry`. Inside the neighbor loop, when `visited[edge.Source]` is already true AND `edge.Source != root`, append a `CycleEntry{From: edge.Source, To: cur.id}` to a local `cycles` slice. Return cycles as third return value.

4. Update `executeForwardTraversal` the same way: when `visited[edge.Target]` is already true AND `edge.Target != root`, append `CycleEntry{From: cur.id, To: edge.Target}`. Return cycles as third return value.

5. Update all callers of these functions (the impact/dependencies query command handlers) to:
   - Accept the third `[]CycleEntry` return value
   - Pass it into `buildMetadata` or set it on the metadata struct before JSON serialization

6. Ensure `buildMetadata` or the query handler sets `metadata.CyclesDetected` from the returned cycles slice. When the slice is nil, `omitempty` ensures the field is absent from JSON.

Run tests — they must PASS.

**REFACTOR phase (if needed):** Clean up any duplication in cycle detection logic between the two BFS functions.
  </action>
  <verify>
    <automated>cd /Users/flurryhead/Developer/Opensource/graphmd && go test ./internal/knowledge/ -run "Cycle|Impact|Dependencies" -v -count=1</automated>
    <manual>Verify cycles_detected appears in JSON output for cyclic graphs, absent for acyclic</manual>
  </verify>
  <done>
    - CycleEntry struct defined with json tags
    - QueryEnvelopeMetadata has CyclesDetected field with omitempty
    - executeImpactReverse detects and returns cycle back-edges (excluding root)
    - executeForwardTraversal detects and returns cycle back-edges (excluding root)
    - Query handlers pass cycle info into JSON envelope metadata
    - Tests verify cycle detection on cyclic graphs, absence on acyclic graphs, correct BFS distances
    - `go build ./...` and `go test ./...` pass
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` succeeds
2. `go test ./internal/knowledge/ -v -count=1` passes all tests including new cycle tests
3. `go vet ./...` clean
4. JSON output from impact/dependencies on cyclic graph includes `cycles_detected` array
5. JSON output on acyclic graph has no `cycles_detected` field
6. No existing test regressions
</verification>

<success_criteria>
- Impact and dependencies queries on graphs with cycles include `cycles_detected` in JSON metadata
- Queries on acyclic graphs produce identical output to current behavior (no cycles_detected field)
- BFS traversal correctness preserved (distances, node ordering)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-silent-loss-reporting/07-01-SUMMARY.md`
</output>
