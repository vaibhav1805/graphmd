---
phase: 07-silent-loss-reporting
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - internal/knowledge/db.go
  - internal/knowledge/db_test.go
autonomous: true
requirements: [DEBT-03]

must_haves:
  truths:
    - "SaveGraph prints a warning to stderr when edges are dropped due to missing endpoint nodes"
    - "Warning includes the count of dropped edges"
    - "Warning includes the source->target pairs of dropped edges"
    - "Warning goes to stderr only (stdout unaffected for JSON parsing)"
    - "SaveGraph return signature is unchanged (still returns error only)"
    - "Existing callers of SaveGraph require no changes"
  artifacts:
    - path: "internal/knowledge/db.go"
      provides: "Dropped edge counting and stderr warning in SaveGraph"
      contains: "dropped"
    - path: "internal/knowledge/db_test.go"
      provides: "Tests for dropped edge warning behavior"
      contains: "TestSaveGraph_DroppedEdge"
  key_links:
    - from: "internal/knowledge/db.go"
      to: "os.Stderr"
      via: "fmt.Fprintf warning after transaction completes"
      pattern: "fmt.Fprintf.*Stderr.*dropped"
---

<objective>
Add stderr warnings when SaveGraph drops edges due to missing endpoint nodes.

Purpose: Operators need visibility into data quality issues — currently edges with missing source/target nodes are silently skipped via `continue` statements, hiding potential graph extraction problems.
Output: Stderr warnings with count and details of dropped edges, no signature changes to SaveGraph.
</objective>

<execution_context>
@/Users/flurryhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flurryhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-silent-loss-reporting/07-RESEARCH.md
@internal/knowledge/db.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dropped edge counting and stderr warnings to SaveGraph</name>
  <files>internal/knowledge/db.go, internal/knowledge/db_test.go</files>
  <action>
**RED phase — write failing tests first:**

In `db_test.go`, write tests for dropped edge warning behavior:

1. `TestSaveGraph_DroppedEdgeWarning`: Create a graph with nodes A and B, plus an edge referencing a non-existent node C (e.g., edge from A to C). Call SaveGraph. Capture stderr output. Assert:
   - SaveGraph returns nil (no error — dropping edges is not an error)
   - Stderr contains "dropped" and the count ("1")
   - Stderr contains the edge pair description (A -> C or similar)
   - The valid edge (A -> B) is still saved successfully

2. `TestSaveGraph_NoDroppedEdges`: Create a graph where all edges reference existing nodes. Call SaveGraph. Assert stderr has no "dropped" warning.

3. `TestSaveGraph_DroppedEdgeMissingSource`: Edge where Source node doesn't exist. Assert warning mentions "missing source".

4. `TestSaveGraph_DroppedEdgeMissingTarget`: Edge where Target node doesn't exist. Assert warning mentions "missing target".

To capture stderr in tests, use an `io.Writer` approach. The simplest pattern: add a package-level `var stderrWriter io.Writer = os.Stderr` in db.go, use `fmt.Fprintf(stderrWriter, ...)` for warnings, and override `stderrWriter` in tests with a `bytes.Buffer`. Alternatively, temporarily redirect os.Stderr in the test using `os.Pipe()`.

Run tests — they must FAIL (SaveGraph doesn't produce warnings yet).

**GREEN phase — implement:**

In `db.go`, inside the `SaveGraph` method:

1. Add a package-level `var stderrWriter io.Writer = os.Stderr` at the top of db.go (near other vars) so tests can capture output.

2. Inside the transaction closure in SaveGraph, before the existing `continue` statements at the edge-skipping lines (around lines 926-931 per research), add tracking:
   - Declare `var droppedCount int` and `var droppedPairs []string` before the edge loop
   - Before the first `continue` (missing source node): increment `droppedCount`, append a description string like `"  source_name -> target_name (missing source \"source_name\")"` to `droppedPairs`
   - Before the second `continue` (missing target node): same pattern with "missing target"

3. After the transaction closure returns successfully (after `transaction()` call returns nil), print the warning:
```go
if droppedCount > 0 {
    fmt.Fprintf(stderrWriter, "  Warning: %d edge(s) dropped (missing endpoint nodes)\n", droppedCount)
    for _, pair := range droppedPairs {
        fmt.Fprintf(stderrWriter, "%s\n", pair)
    }
}
```

IMPORTANT: The `droppedCount` and `droppedPairs` variables must be declared OUTSIDE the transaction closure but populated INSIDE it, so they survive after the closure returns. Alternatively, declare them before the `transaction()` call and use them in the closure via closure capture.

4. Do NOT change the SaveGraph return signature — it stays as `func (db *Database) SaveGraph(graph *Graph) error`.

Run tests — they must PASS.

**REFACTOR phase (if needed):** Ensure warning format is clean and consistent with existing stderr patterns in the codebase.
  </action>
  <verify>
    <automated>cd /Users/flurryhead/Developer/Opensource/graphmd && go test ./internal/knowledge/ -run "TestSaveGraph" -v -count=1</automated>
    <manual>Run graphmd export on test data with intentionally bad edges and verify stderr warning appears</manual>
  </verify>
  <done>
    - SaveGraph prints "Warning: N edge(s) dropped" to stderr when edges are skipped
    - Each dropped edge pair is listed with reason (missing source/target)
    - Warning only appears after successful transaction (not on rollback)
    - SaveGraph return signature unchanged
    - No caller changes needed (main.go, export.go, command_helpers.go all work unchanged)
    - `go build ./...` and `go test ./...` pass
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` succeeds
2. `go test ./internal/knowledge/ -v -count=1` passes all tests including new dropped edge tests
3. `go vet ./...` clean
4. SaveGraph on graph with dangling edges prints warning to stderr
5. SaveGraph on clean graph produces no warning
6. No existing test regressions
7. SaveGraph signature unchanged — `go build ./...` confirms no caller breakage
</verification>

<success_criteria>
- SaveGraph warns to stderr when edges are dropped due to missing endpoint nodes
- Warning includes count and edge pairs with reason
- No changes to SaveGraph return signature or caller code
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-silent-loss-reporting/07-02-SUMMARY.md`
</output>
