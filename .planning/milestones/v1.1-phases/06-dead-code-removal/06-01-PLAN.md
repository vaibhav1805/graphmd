---
phase: 06-dead-code-removal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/knowledge/query.go
  - internal/knowledge/query_test.go
  - internal/knowledge/types.go
  - internal/knowledge/types_test.go
autonomous: true
requirements:
  - DEBT-01

must_haves:
  truths:
    - "`go build ./...` succeeds with no compilation errors"
    - "`go test ./...` passes with no test failures"
    - "`go vet ./...` passes with no issues"
    - "ExecuteImpact, ExecuteCrawl, QueryResult, AffectedNode, QueryEdge are not present anywhere in the codebase"
    - "query.go and query_test.go do not exist in internal/knowledge"
    - "Coverage on live files (graph.go, db.go, query_cli.go) has not regressed"
  artifacts:
    - path: "internal/knowledge/types.go"
      provides: "Live type definitions only — no AffectedNode, QueryEdge, QueryResult"
      contains: "TraversalState"
    - path: "internal/knowledge/types_test.go"
      provides: "Live type tests only — no TestQueryResult_JSONMarshal, TestQueryResult_Validation"
  key_links:
    - from: "internal/knowledge/query_cli.go"
      to: "internal/knowledge/types.go"
      via: "ImpactResult, ImpactNode, EnrichedRelationship types (live CLI types remain intact)"
      pattern: "ImpactResult|ImpactNode|EnrichedRelationship"
---

<objective>
Remove all orphaned query execution code from the Phase 2 era that was superseded by Phase 4's CLI query implementation. Delete query.go and query_test.go entirely. Surgically remove AffectedNode, QueryEdge, QueryResult (plus String/Validate methods) from types.go, and their tests from types_test.go.

Purpose: Eliminate ~696 lines of dead code so the query layer has one unambiguous type hierarchy with no orphaned parallel implementations.
Output: Clean codebase with zero orphaned query types/functions, verified by build + test + vet + grep.
</objective>

<execution_context>
@/Users/flurryhead/.claude/get-shit-done/workflows/execute-plan.md
@/Users/flurryhead/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-dead-code-removal/06-RESEARCH.md
@internal/knowledge/query.go
@internal/knowledge/query_test.go
@internal/knowledge/types.go
@internal/knowledge/types_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture baseline and perform all deletions</name>
  <files>
    internal/knowledge/query.go
    internal/knowledge/query_test.go
    internal/knowledge/types.go
    internal/knowledge/types_test.go
  </files>
  <action>
    Step 1 — Capture baseline coverage for comparison:
    Run `go test -cover ./internal/knowledge/ 2>&1` and record the coverage percentage (expected ~52.7%). Also record per-file coverage if available, focusing on graph.go, db.go, query_cli.go, types.go.

    Step 2 — Delete entire files:
    - `rm internal/knowledge/query.go` (224 lines — contains ExecuteImpact, ExecuteCrawl, ImpactQuery, CrawlQuery, buildQueryResult, computeDistances, passesConfidenceFilter)
    - `rm internal/knowledge/query_test.go` (308 lines — contains all tests for the above)

    Step 3 — Surgical removal from types.go:
    Remove the AffectedNode struct, QueryEdge struct, QueryResult struct, QueryResult.String() method, and QueryResult.Validate() method. These are approximately lines 371-450 (after TraversalState). Keep everything above the AffectedNode definition. After removal, check if any imports became unused (likely "encoding/json" or "fmt" or "strings") and remove them if so. Run `go build ./...` immediately to catch import issues.

    Step 4 — Surgical removal from types_test.go:
    Remove TestQueryResult_JSONMarshal and TestQueryResult_Validation (approximately lines 171-254). Keep TestRelationshipLocationKey_Deterministic and everything else. Check for unused imports after removal.

    Step 5 — Verify build, test, and vet:
    - `go build ./...` — must succeed
    - `go test ./...` — must pass
    - `go vet ./...` — must pass
    - `go test -cover ./internal/knowledge/` — compare coverage on live files against baseline
  </action>
  <verify>
    <automated>cd /Users/flurryhead/Developer/Opensource/graphmd && go build ./... && go test ./... && go vet ./...</automated>
  </verify>
  <done>
    query.go and query_test.go deleted. AffectedNode, QueryEdge, QueryResult types and methods removed from types.go. Corresponding tests removed from types_test.go. Build, test, and vet all pass. No unused import errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify zero orphaned references and commit</name>
  <files>
    internal/knowledge/types.go
    internal/knowledge/types_test.go
  </files>
  <action>
    Step 1 — Grep verification across entire codebase:
    Search for all deleted symbol names to confirm zero references remain:
    - ExecuteImpact, ExecuteCrawl, ImpactQuery, CrawlQuery
    - QueryResult (note: pageIndexQueryResult in context.go is unrelated — it's unexported and a different type)
    - AffectedNode (note: result.AffectedNodes in query_cli_test.go refers to ImpactResult.AffectedNodes field, not the deleted AffectedNode type)
    - QueryEdge
    - buildQueryResult, computeDistances, passesConfidenceFilter

    Grep command: `grep -rn "ExecuteImpact\|ExecuteCrawl\|ImpactQuery\|CrawlQuery\|QueryResult\b\|AffectedNode\b\|QueryEdge\|buildQueryResult\|computeDistances\|passesConfidenceFilter" internal/knowledge/*.go`

    Expected: zero matches (or only false positives like pageIndexQueryResult, ImpactResult.AffectedNodes which are different symbols).

    If any real references found, investigate and fix before committing.

    Step 2 — Atomic commit:
    Stage all changes and commit with a message listing what was removed. Per user decision, this must be one atomic commit. The commit message should list the removed symbols.
  </action>
  <verify>
    <automated>cd /Users/flurryhead/Developer/Opensource/graphmd && grep -rn "ExecuteImpact\|ExecuteCrawl\|\bImpactQuery\b\|\bCrawlQuery\b\|\bQueryResult\b\|\bAffectedNode\b\|\bQueryEdge\b\|buildQueryResult\|computeDistances\|passesConfidenceFilter" internal/knowledge/*.go | grep -v pageIndexQueryResult | grep -v "AffectedNodes" || echo "CLEAN: no orphaned references"</automated>
  </verify>
  <done>
    Zero orphaned references to deleted symbols in the codebase. All deletions committed in one atomic commit with descriptive message listing removed symbols.
  </done>
</task>

</tasks>

<verification>
1. `go build ./...` succeeds
2. `go test ./...` passes with no failures
3. `go vet ./...` passes
4. `ls internal/knowledge/query.go internal/knowledge/query_test.go 2>&1` returns "No such file"
5. Grep for deleted symbols returns zero real matches
6. Coverage on graph.go, db.go, query_cli.go has not regressed from baseline
</verification>

<success_criteria>
- The query layer has one unambiguous type hierarchy (ImpactResult/ImpactNode/EnrichedRelationship in query_cli.go)
- No orphaned parallel implementation exists (ExecuteImpact/ExecuteCrawl/QueryResult/AffectedNode/QueryEdge all gone)
- ~696 lines of dead code removed
- All Go toolchain checks pass (build, test, vet)
- One atomic commit with clear message
</success_criteria>

<output>
After completion, create `.planning/phases/06-dead-code-removal/06-01-SUMMARY.md`
</output>
